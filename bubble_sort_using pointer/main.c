#include <stdio.h>
#include <stdlib.h>

int main()
{
   int j,i,n;
   int *ptr;
   int tmp;
   for(j=0;j<n-1;i++)
    for(i=0;i<n-2;i++)
    if((*ptr)>(*ptr1))
        tmp=(*ptr);
        *ptr=(*(ptr+1));
        *(ptr+1)=tmp;
    return 0;
}
